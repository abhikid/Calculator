// Mathematical constants available as identifiers in expressions

export const CONSTANTS: Record<string, number> = {
  pi: Math.PI,
  π: Math.PI,
  e: Math.E,
  phi: (1 + Math.sqrt(5)) / 2, // golden ratio
  inf: Infinity,
}
